apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
  namespace: holmes-demo
  labels: {app: pending-pod}
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
      resources:
        requests: {cpu: "999999", memory: 1Ti}

