image:
  repository: ghcr.io/gunasekar89/agiops-k8
  tag: latest
  pullPolicy: IfNotPresent
target:
  namespace: holmes-demo
  pod: ""
providerSecret:
  name: holmes-provider-credentials
  key: OPENAI_API_KEY
serviceAccount:
  name: holmes-console
healer:
  enabled: false
  mode: observe
  maxRiskScore: 25
  webhookTokenSecretKey: WEBHOOK_TOKEN
