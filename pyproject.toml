[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis-holmes"
version = "0.2.0"
description = "A read-only Kubernetes investigation console powered by HolmesGPT"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
authors = [{name = "Aegis Holmes contributors"}]
dependencies = [
    "fastapi>=0.115,<1",
    "rich>=13.7,<15",
    "uvicorn>=0.30,<1",
]
keywords = ["kubernetes", "holmesgpt", "aiops", "troubleshooting"]

[project.optional-dependencies]
dev = ["httpx>=0.27", "pytest>=7.4", "pytest-cov>=4.1", "ruff>=0.5"]

[project.scripts]
holmes-console = "holmes_console.cli:main"
aegis-holmes = "holmes_console.cli:main"
alert-healer = "holmes_console.server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["src"]
